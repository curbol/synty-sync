# Account-specific config (customer id, machine paths) — never committed.
config.local.toml
config.toml

# Captured browser sessions (contain cookies) — never committed.
session.curl
*.curl
cookies.txt

# Per-user library state belongs outside the tool repo (see --config); ignored here
# defensively so it never lands in the repo even when run from this directory.
packs.toml
synty-library.lock.json

# Built binary.
/synty-sync
